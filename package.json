{
  "name": "zotero-refs",
  "type": "module",
  "version": "1.1.1",
  "description": "Refs — references, citations, related papers and a citation graph for every Zotero item",
  "config": {
    "addonName": "Refs",
    "addonID": "refs@zotero-refs.app",
    "addonRef": "refs",
    "addonInstance": "Refs",
    "prefsPrefix": "extensions.zotero.refs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yimmy23/zotero-refs.git"
  },
  "author": "yim",
  "bugs": {
    "url": "https://github.com/yimmy23/zotero-refs/issues"
  },
  "homepage": "https://github.com/yimmy23/zotero-refs#readme",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "start": "zotero-plugin serve",
    "build": "zotero-plugin build && tsc --noEmit",
    "lint:check": "prettier --check . && eslint .",
    "lint:fix": "prettier --write . && eslint . --fix",
    "release": "zotero-plugin release",
    "test": "zotero-plugin test",
    "update-deps": "npm update --save",
    "test:offline": "node scripts/core-regression.mjs && node scripts/text-regression.mjs && node scripts/abstract-regression.mjs && node scripts/parser-regression.mjs && node scripts/reader-regression.mjs && node scripts/ui-regression.mjs && node scripts/popup-regression.mjs && node scripts/view-regression.mjs && node scripts/pubmed-regression.mjs && node scripts/source-regression.mjs",
    "check": "npm run build && npm run test:offline"
  },
  "dependencies": {
    "d3-force": "^3.0.0",
    "zotero-plugin-toolkit": "^5.2.0"
  },
  "devDependencies": {
    "@types/d3-force": "^3.0.10",
    "@types/node": "^26.4.0",
    "@xmldom/xmldom": "^0.9.12",
    "@zotero-plugin/eslint-config": "^0.6.7",
    "eslint": "^10.9.1",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3",
    "zotero-plugin-scaffold": "^0.8.8",
    "zotero-types": "^4.1.3"
  },
  "prettier": {
    "printWidth": 80,
    "tabWidth": 2,
    "endOfLine": "lf",
    "overrides": [
      {
        "files": [
          "*.xhtml"
        ],
        "options": {
          "htmlWhitespaceSensitivity": "css"
        }
      }
    ]
  }
}
